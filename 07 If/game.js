let player = ""
let computer = ""

if (player === computer) {

    // draw

} else if (player === "rock") {

    if (computer === "paper") {
        // computer wins
    } else {
        // player wins
    }

}
else if (player === "paper") {

    if (computer === "scisors") {
        // computer wins
    } else {
        // player wins
    }

}
else if (player === "scisor") {

    if (computer === "rock") {
        // computer wins
    } else {
        //  player wins
    }

}